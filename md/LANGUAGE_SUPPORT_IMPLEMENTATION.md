# การรองรับ 2 ภาษา (อังกฤษ/ไทย) สำหรับหน้า Login และ Admin

## การเปลี่ยนแปลงที่ทำ

### 1. **translations.js** - เพิ่มคำแปล
เพิ่มคำแปลภาษาไทยและภาษาอังกฤษสำหรับหน้า Login และ Admin:
- คำแปลหน้า Login (ชื่อผู้ใช้, รหัสผ่าน, ปุ่มต่างๆ)
- คำแปลหน้า Admin (เมนู, ตาราง, ฟอร์ม, สถานะ)
- ระบบจัดการภาษา (Language Management Functions)
  - `setLanguage(lang)` - เปลี่ยนภาษา
  - `updatePageLanguage()` - อัพเดทเนื้อหาทั้งหน้า
  - บันทึกภาษาที่เลือกใน `localStorage`

### 2. **login.html** - อัพเดทโครงสร้าง
- เปลี่ยน `lang="th"` เป็น `lang="en"` (ค่าเริ่มต้น)
- เพิ่มปุ่มสลับภาษา (`<button class="lang-toggle">`)
- เพิ่ม `data-i18n` attributes ให้กับทุก element ที่ต้องแปล:
  - หัวข้อและคำบรรยาย
  - Label ของ input fields
  - Placeholder text
  - ปุ่มและลิงก์
- เพิ่ม `<script src="translations.js">` ก่อน `login.js`

### 3. **login.js** - เพิ่มฟังก์ชันภาษา
- เพิ่ม `initLanguage()` - โหลดภาษาที่บันทึกไว้
- เพิ่ม `toggleLanguage()` - สลับระหว่างภาษาไทยและอังกฤษ
- ใช้ `translations[currentLang]` สำหรับข้อความ error และ success

### 4. **login.css** - CSS ปุ่มสลับภาษา
```css
.lang-toggle {
    position: fixed;
    top: 20px;
    right: 20px;
    z-index: 1000;
    /* สไตล์สวยงาม พร้อมเอฟเฟกต์ hover */
}
```

### 5. **admin.html** - อัพเดทโครงสร้าง
- เปลี่ยน `lang="th"` เป็น `lang="en"` (ค่าเริ่มต้น)
- เพิ่มปุ่มสลับภาษา
- เพิ่ม `data-i18n` attributes ทั่วทั้งหน้า:
  - Sidebar menu items
  - Dashboard stats cards
  - Table headers (ทุกตาราง)
  - Filter labels
  - Modal titles และ buttons
  - Form fields
- เพิ่ม `<script src="translations.js">` ก่อน `admin.js`

### 6. **admin.js** - เพิ่มฟังก์ชันภาษา
- เพิ่ม `initLanguage()` ใน `DOMContentLoaded`
- เพิ่ม `toggleLanguage()` function

### 7. **admin.css** - CSS ปุ่มสลับภาษา
```css
.lang-toggle {
    position: fixed;
    top: 20px;
    right: 20px;
    z-index: 1001;
    /* สไตล์สวยงาม พร้อมเอฟเฟกต์ hover */
}
```

## วิธีการทำงาน

### การเปลี่ยนภาษา
1. คลิกปุ่ม **"TH"** หรือ **"EN"** ที่มุมบนขวา
2. ระบบจะ:
   - บันทึกภาษาที่เลือกใน `localStorage`
   - อัพเดท `currentLang` variable
   - เรียก `updatePageLanguage()` เพื่ออัพเดทเนื้อหาทั้งหน้า
   - เปลี่ยนข้อความในปุ่มเป็นภาษาตรงข้าม

### ระบบแปล (Translation System)
```javascript
// ตัวอย่างการใช้ data-i18n
<h1 data-i18n="loginTitle">Admin Dashboard</h1>

// เมื่อเปลี่ยนภาษา ระบบจะ:
element.textContent = translations[currentLang].loginTitle;
// EN: "Admin Dashboard"
// TH: "Admin Dashboard" (เหมือนเดิมในกรณีนี้)
```

## คำแปลที่เพิ่ม (ตัวอย่าง)

### Login Page
| Key | English | ไทย |
|-----|---------|-----|
| `username` | Username | ชื่อผู้ใช้ |
| `password` | Password | รหัสผ่าน |
| `rememberMe` | Remember me | จดจำฉันไว้ |
| `login` | Login | เข้าสู่ระบบ |
| `backToHomepage` | Back to Homepage | กลับหน้าแรก |

### Admin Page
| Key | English | ไทย |
|-----|---------|-----|
| `dashboard` | Dashboard | แดชบอร์ด |
| `bookings` | Bookings | การจอง |
| `manageUsers` | Manage Users | จัดการ Users |
| `todayBookings` | Today's Bookings | การจองวันนี้ |
| `pending` | Pending | รอยืนยัน |
| `confirmed` | Confirmed | ยืนยันแล้ว |

## ภาษาเริ่มต้น (Default Language)
- ✅ **ภาษาอังกฤษ (English)** - ตามที่ร้องขอ
- ภาษาจะถูกบันทึกใน `localStorage` key: `"language"`
- ครั้งแรกที่เปิดหน้า: แสดงภาษาอังกฤษ
- ครั้งต่อไป: แสดงภาษาที่เลือกไว้ล่าสุด

## การทดสอบ

### ทดสอบหน้า Login
1. เปิด `login.html`
2. ตรวจสอบว่าข้อความทั้งหมดแสดงเป็นภาษาอังกฤษ
3. คลิกปุ่ม **"TH"** → ข้อความเปลี่ยนเป็นภาษาไทย
4. คลิกปุ่ม **"EN"** → ข้อความกลับเป็นภาษาอังกฤษ

### ทดสอบหน้า Admin
1. Login เข้าสู่ระบบ
2. ตรวจสอบว่าข้อความทั้งหมดแสดงเป็นภาษาอังกฤษ
3. สลับภาษาโดยคลิกปุ่มที่มุมบนขวา
4. ตรวจสอบการแปลใน:
   - Sidebar menu
   - Dashboard cards
   - Tables (Recent Bookings, Bookings Page, Users Page)
   - Modals (Booking Details, User Form)
   - Filters และ dropdowns

## Features เพิ่มเติม
- 🎨 ปุ่มสลับภาษาสวยงาม พร้อมเอฟเฟกต์ hover
- 💾 บันทึกภาษาที่เลือกอัตโนมัติ
- 🔄 แปลภาษาทันทีโดยไม่ต้อง reload หน้า
- 📱 รองรับทุกหน้าจอ (Responsive)

## ไฟล์ที่แก้ไข
1. ✅ `translations.js` - เพิ่มคำแปลและระบบจัดการภาษา
2. ✅ `login.html` - เพิ่ม data-i18n attributes
3. ✅ `login.js` - เพิ่มฟังก์ชันการแปลภาษา
4. ✅ `login.css` - สไตล์ปุ่มสลับภาษา
5. ✅ `admin.html` - เพิ่ม data-i18n attributes
6. ✅ `admin.js` - เพิ่มฟังก์ชันการแปลภาษา
7. ✅ `admin.css` - สไตล์ปุ่มสลับภาษา

---

## สรุป
ระบบรองรับ 2 ภาษา (อังกฤษและไทย) สำหรับหน้า Login และ Admin ได้สำเร็จแล้ว โดยใช้ภาษาอังกฤษเป็นค่าเริ่มต้นตามที่ร้องขอ ผู้ใช้สามารถสลับภาษาได้ง่ายด้วยปุ่มที่มุมบนขวาของหน้า 🎉
