# 🎯 การปรับปรุงระบบการจอง (Booking System Improvements)

## สรุปการแก้ไข (Summary of Changes)

เราได้ปรับปรุงระบบการจองของ Wargames Holiday Centre Phuket ให้มีประสิทธิภาพและใช้งานง่ายขึ้น

---

## ✨ ฟีเจอร์ใหม่ที่เพิ่มเข้ามา (New Features)

### 1. 📅 แสดงระยะเวลาพักอัตโนมัติ (Automatic Duration Display)
- คำนวณจำนวนคืนโดยอัตโนมัติจากวันเช็คอินและเช็คเอาท์
- แสดงผลเป็นภาษาไทยและอังกฤษตาม language preference
- อัพเดทแบบ real-time เมื่อเปลี่ยนวันที่

### 2. 💰 ประมาณการราคา (Price Estimation)
- คำนวณราคาโดยประมาณตาม:
  - ประเภทแพ็คเกจ (Campaign Weekend, Own Hosted, Custom)
  - ระดับที่พัก (Basic, Superior, Deluxe)
  - จำนวนผู้ใหญ่และเด็ก (เด็กได้ส่วนลด 30%)
  - จำนวนคืนที่พัก
- แสดงราคาเป็นบาทไทยพร้อมคำเตือนว่าเป็นราคาประมาณการ

**ราคาฐาน (Base Prices):**
- Campaign Weekend: ฿8,500/คน/คืน
- Own Hosted Weekend: ฿7,500/คน/คืน
- Custom Package: ฿9,000/คน/คืน

**ค่าเพิ่มที่พัก (Accommodation Surcharge):**
- Basic: ไม่มีค่าเพิ่ม
- Superior: +฿1,500/คน/คืน
- Deluxe/Hot Spring: +฿3,000/คน/คืน

### 3. ✅ การตรวจสอบข้อมูล (Data Validation)
- **วันที่**: 
  - ตรวจสอบว่าวันเช็คอินต้องไม่เป็นวันที่ผ่านมาแล้ว
  - วันเช็คเอาท์ต้องมาหลังวันเช็คอิน
  - ป้องกันไม่ให้เลือกวันเช็คเอาท์ก่อนวันเช็คอิน
- **อีเมล**: ตรวจสอบรูปแบบอีเมลให้ถูกต้อง
- **เบอร์โทรศัพท์**: ตรวจสอบว่าเป็นตัวเลขและสัญลักษณ์ที่อนุญาต

### 4. 🔒 ยืนยันก่อนปิดฟอร์ม (Confirm Before Close)
- ถ้ามีข้อมูลที่กรอกแล้วในฟอร์ม จะแจ้งเตือนก่อนปิด
- ป้องกันการสูญเสียข้อมูลโดยไม่ตั้งใจ

### 5. 📧 จัดรูปแบบข้อมูลสำหรับอีเมล (Email Formatting)
- จัดรูปแบบข้อมูลการจองให้อ่านง่าย
- แสดงข้อมูลครบถ้วน:
  - ข้อมูลส่วนตัว
  - รายละเอียดการจอง (รวมจำนวนคืน)
  - บริการเสริม
  - คำขอพิเศษ
  - เวลาที่ส่งและภาษาที่ใช้

### 6. 🔄 ระบบ Loading State
- แสดงสถานะ "กำลังส่ง..." เมื่อกดส่งฟอร์ม
- ปิดการใช้งานปุ่มระหว่างส่งข้อมูล
- แสดงข้อความเป็นภาษาตามที่เลือก

### 7. ⚠️ Error Handling
- จัดการข้อผิดพลาดอย่างเหมาะสม
- แสดงข้อความผิดพลาดเป็นภาษาไทยหรืออังกฤษ
- รีเซ็ตสถานะของฟอร์มหลังเกิดข้อผิดพลาด

---

## 🛠️ การใช้งาน (How to Use)

### สำหรับผู้ใช้ทั่วไป:
1. คลิกปุ่ม "จองเลย" หรือ "Book Now" ที่ใดก็ได้ในเว็บไซต์
2. กรอกข้อมูลส่วนตัว (ชื่อ, อีเมล, เบอร์โทร, ประเทศ)
3. เลือกอีเวนต์และประเภทแพ็คเกจ
4. เลือกวันเช็คอินและเช็คเอาท์ → จะแสดงจำนวนคืนอัตโนมัติ
5. เลือกจำนวนผู้ใหญ่และเด็ก
6. เลือกระดับที่พัก → จะแสดงราคาประมาณการ
7. เลือกบริการเสริม (ถ้าต้องการ)
8. กรอกคำขอพิเศษ (ถ้ามี)
9. กด "ส่งคำขอจอง" → ระบบจะแสดงหน้าต่างยืนยัน

### สำหรับการจองจากอีเวนต์:
1. คลิกดูรายละเอียดอีเวนต์
2. คลิก "Register for Event"
3. ข้อมูลอีเวนต์จะถูกเลือกไว้ล่วงหน้า
4. กรอกข้อมูลที่เหลือตามปกติ

---

## 🔧 สำหรับนักพัฒนา (For Developers)

### ฟังก์ชันหลักที่เพิ่มเข้ามา:

```javascript
// แสดงระยะเวลาพัก
updateBookingDuration()

// คำนวณและแสดงราคาประมาณการ
updatePriceEstimate()

// ตรวจสอบข้อมูลในฟอร์ม
validateBookingForm()

// จัดรูปแบบข้อมูลสำหรับอีเมล
formatBookingEmail(data)

// ตรวจสอบว่าฟอร์มมีข้อมูลหรือไม่
hasFormData()
```

### การเชื่อมต่อ Backend (ในอนาคต):

ในไฟล์ `script.js` บรรทัดที่มี comment:
```javascript
// Option 2: In production, send to your server:
```

แก้เป็น:
```javascript
const response = await fetch('YOUR_API_ENDPOINT/api/bookings', {
    method: 'POST',
    headers: {
        'Content-Type': 'application/json',
    },
    body: JSON.stringify(bookingData)
});

if (!response.ok) {
    throw new Error('Failed to submit booking');
}
```

### ปรับแต่งราคา:

แก้ไขในฟังก์ชัน `updatePriceEstimate()`:
```javascript
// Base prices (adjust according to your actual pricing)
let basePrice = 0;
switch(packageType) {
    case 'campaign-weekend':
        basePrice = 8500; // เปลี่ยนตามราคาจริง
        break;
    // ...
}
```

---

## 📝 Todo List (สิ่งที่ควรทำต่อไป)

- [ ] เชื่อมต่อกับ backend API จริง
- [ ] เพิ่มระบบชำระเงินออนไลน์
- [ ] เพิ่มการส่งอีเมลยืนยันอัตโนมัติ
- [ ] เพิ่ม Google Calendar integration
- [ ] สร้าง admin dashboard สำหรับจัดการการจอง
- [ ] เพิ่มระบบ promo code / discount code
- [ ] เพิ่มการแสดงห้องที่ว่างแบบ real-time
- [ ] Export ข้อมูลการจองเป็น PDF
- [ ] เพิ่ม SMS notification

---

## 🐛 Bug Fixes

1. แก้ไขปัญหาที่ข้อมูล `selectedEvent` ไม่ถูกส่งไปใน booking data
2. แก้ไขปัญหาการแสดงภาษาในปุ่ม loading state
3. เพิ่มการป้องกันการสูญเสียข้อมูลเมื่อปิดฟอร์มโดยไม่ตั้งใจ
4. แก้ไขการรีเซ็ต event select หลังส่งฟอร์มสำเร็จ

---

## 📞 ข้อมูลติดต่อ

หากพบปัญหาหรือต้องการความช่วยเหลือ:
- Email: info@wargameshc.com
- Phone: +66 XX XXX XXXX

---

**เวอร์ชัน:** 2.0  
**วันที่อัพเดท:** 27 ธันวาคม 2025  
**อัพเดทโดย:** GitHub Copilot
